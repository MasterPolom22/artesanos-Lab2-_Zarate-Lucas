extends layout

block content
  .container.mt-5.text-center
    h1.display-4.text-danger 404 - Página no encontrada
    p.lead La página que estás buscando no existe o fue movida.
    a.btn.btn-primary.mt-3(href="/") Volver al inicio
